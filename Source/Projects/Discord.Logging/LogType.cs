namespace Discord.Logging
{
    /// <summary>
    /// Enumerates the different types of messages supported by the logging system.
    /// </summary>
    public enum LogType
    {
        Error,
        Warning,
        Info,
        Debug
    }
}
